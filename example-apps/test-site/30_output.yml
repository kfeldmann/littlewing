target-groups:
  build:
  - build-${var.environment}-${var.tier}
  build-${var.environment}:
  - build-${var.environment}-${var.tier}

steps.build-${var.environment}-${var.tier}:
- print: "URL: http://${aws.elb.create-load-balancer.dev-website-web-0.DNSName}/"
